# Specialist repair worker v2

System-script GitHub issues with a verified `scripts/*.py` target are routed through `scripts/specialist_issue_autorepair.py`.

The wrapper grounds the coding model on the explicit safe script target and its conventional companion test before any package-code fallback. Protected scripts remain excluded. The v2 worker preserves the existing privileged candidate, validation, exact-scope, and post-promotion full-suite checks, while allowing enough bounded model output for a complete repair JSON.
