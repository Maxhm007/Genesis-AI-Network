# Disabled: the original closed-loop proof has completed its purpose.
# Kept only as an inert manual marker. It cannot create test issues, write
# detector heartbeats, mutate repository files, or self-dispatch successor runs.
name: Genesis Tiny Problem Detection Test

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: genesis-tiny-problem-detection-test
  cancel-in-progress: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Tiny test disabled
        run: echo "Genesis Tiny Problem Detection Test is disabled."
