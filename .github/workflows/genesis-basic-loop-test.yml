# Disabled: the original closed-loop proof has completed its purpose.
# Keep this workflow only as an inert manual marker so it cannot create test issues,
# write detector heartbeats, mutate repository files, or self-dispatch successor runs.
name: Genesis Tiny Problem Detection Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Tiny test disabled
        run: echo "Genesis Tiny Problem Detection Test is disabled."
