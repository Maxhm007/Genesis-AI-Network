# Disabled: the original closed-loop proof has completed its purpose.
# This temporary inert push trigger uses the original concurrency group so an
# already-running legacy detector is cancelled without creating new test work.
name: Genesis Tiny Problem Detection Test

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/genesis-basic-loop-test.yml

permissions:
  contents: read

concurrency:
  group: genesis-tiny-problem-detection-test
  cancel-in-progress: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Tiny test disabled
        run: echo "Genesis Tiny Problem Detection Test is disabled."
